# Repository Setup

Target repository (public, currently empty — a clean build target):

https://github.com/kendallmark3/intent-driven-plugin

## Steps

```bash
git clone https://github.com/kendallmark3/intent-driven-plugin.git
cd intent-driven-plugin

git checkout -b feature/initial-plugin
code .

claude
```

Save the combined intent as `INTENT.md` in the repository root before launching Claude Code.
